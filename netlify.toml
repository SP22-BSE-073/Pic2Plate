[build]
  command = "npm run build"

[functions]
  directory = "backend"
  included_files = [
    "backend/server.js",
    "backend/**", # Ensures all backend files are deployed
    "!backend/public/**" # Exclude static files as they're served separately
  ]
  node_bundler = "esbuild"

[[plugins]]
  package = "@netlify/plugin-functions-install-core"
